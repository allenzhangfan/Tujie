apply from:"../common_component_build.gradle"

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation project(':component_cameraview')
    implementation project(':component_cropper')
}